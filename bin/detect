#!/usr/bin/env bash
#
# bin/detect <build-dir>
#
# The script is called with one argument, the build directory for the app.
# The build directory contains the app files uploaded when you run cf push.
#
# The detect script returns an exit code of 0 if the buildpack is compatible with
# the app. In the case of system buildpacks, the script also prints the buildpack
# name, version, and other information to STDOUT.
#
#
echo "bin/detect running"
env
exit 0